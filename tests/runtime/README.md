# Runtime compatibility package

The active runtime checks were split into functional sibling packages:

- `runtime_core`
- `runtime_repository`
- `runtime_translation`
- `runtime_workspace`
- `runtime_ib`
- `runtime_ctrader`
- `runtime_orders`
- `runtime_manual`

This package is intentionally kept as an empty compatibility anchor.
